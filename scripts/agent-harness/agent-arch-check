#!/usr/bin/env sh
# Architecture import-boundary check. Generated by company-agent-harness v0.4.0.
# Rules are configured in .agent-harness.yml -> architecture.forbidden_imports
# (each rule: { from: "<dir>", pattern: "<forbidden substring>" }).

if command -v agent-harness >/dev/null 2>&1; then
  agent-harness verify
elif command -v npx >/dev/null 2>&1; then
  npx --yes https://github.com/fwani/agent-harness.git verify
else
  echo "agent-harness not found."
  echo "Install it (npm i -g https://github.com/fwani/agent-harness.git) to run architecture checks,"
  echo "or review .agent-harness.yml architecture.forbidden_imports manually."
fi
